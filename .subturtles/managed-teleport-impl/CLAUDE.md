# Current task

Continue the hosted auth foundation by tightening durable hosted session semantics for `superturtle login` / `whoami` / `cloud status`, with a focus on the remaining hosted login/session contract work after adding bounded control-plane request timeouts, bounded browser-launch behavior, fail-closed redirect handling, crash-durable local session deletion on logout/invalidation, fail-closed content-type validation for control-plane JSON APIs, fail-closed local logout path validation, no-follow hosted session reads that refuse symlink swaps at open time, fail-closed validation of hosted session writes before anything reaches disk, and HTTPS-only validation for configured and persisted hosted control-plane origins outside loopback so production auth/session traffic cannot be downgraded onto insecure remote HTTP endpoints while local loopback test harnesses still work. Most recently, hosted login polling now caps each sleep against the remaining overall login timeout budget and re-checks the deadline before issuing the next poll request, so hostile or buggy `Retry-After` / `slow_down` backoff instructions cannot stretch `superturtle login` past its configured timeout window before failing closed. Hosted login-start validation now applies the same loopback-aware origin policy to `verification_uri` and `verification_uri_complete` that the configured control-plane origin already uses, so local browser/device login harnesses can complete over `http://127.0.0.1` while remote hosted login flows still fail closed on insecure non-loopback HTTP endpoints. Hosted session directory creation now `fsync`s the parent directory after each newly created path segment plus the new directory itself before any session-file rename depends on that metadata, and newly created hosted session directories are now created with `0700` permissions so first-time `superturtle login` and other fresh local cloud-session writes do not leave newly minted credential directories world-readable on permissive umasks; hosted session post-write and post-delete durability sync already reopen the final session path and parent directory through no-follow descriptors plus post-open type validation before `fsync`, so a symlink swap after atomic rename or unlink cannot redirect the crash-durability step onto an attacker-controlled file or directory; hosted session reads now use a bounded descriptor-level read loop after `fstat` instead of an unbounded `readFileSync(fd, ...)`, so a concurrent file-growth race after the initial size check still fails closed against the configured session-file byte cap rather than pulling an oversized local session blob into memory; authenticated hosted session reads already fail closed on control-plane `403` responses as well as `401`, immediately clearing the local cloud session instead of leaving a rejected bearer session cached for later `whoami` / `cloud status` reuse; hosted session writes now validate the normalized session schema before persistence and reject oversized serialized session payloads against the configured local session-file byte limit before any temp file is created, so malformed or oversized login/refresh/cache updates cannot leave behind a partially trusted local cloud session file; hosted session writes already `fsync` the temp file before rename plus the final session file and parent directory after replacement so successful login/refresh writes are crash-durable on supported filesystems, hosted session writes already create exclusive random temp files before atomic rename so pre-created predictable temp-path symlinks cannot redirect login or refresh writes, hosted session deletion now also `fsync`s the parent directory after unlink so successful logout/session invalidation does not rely on non-durable metadata updates alone, logout/session invalidation now re-validates the parent config directory plus session file type before unlink so local credential deletion refuses symlinked config directories and symlink session paths instead of traversing or mutating unsafe paths, the existing `lstat`-based session-path and parent-directory validation remains in place to reject symlinked session files and symlinked config directories, hosted session reads now reopen the validated session file through an `O_NOFOLLOW` descriptor plus `fstat` before reading so a symlink swap between validation and file open fails closed instead of being followed on supported platforms, local permission repair now also reopens the validated hosted session file through an `O_NOFOLLOW` descriptor plus `fstat` before `fchmod`, so a symlink swap between read-time validation and `0600` repair fails closed instead of retargeting permission changes onto another file, login polling now treats retryable `429` / `slow_down` control-plane responses as backoff signals instead of aborting the browser login flow, the poller now also honors HTTP `Retry-After` throttling so hosted device login can follow production control-plane rate-limit instructions instead of free-running on the local backoff alone, hosted login/session/status/refresh requests now fail with explicit timeout errors after a configurable bounded wait so dead control-plane connections cannot hang the CLI indefinitely while `whoami` / `cloud status` still fall back to cached snapshots on retryable timeout failures, browser launch during `superturtle login --browser` now uses a configurable bounded timeout so a wedged local opener cannot hang the production login path before device polling begins, hosted control-plane responses plus the local persisted session file now enforce configurable positive byte-size limits so oversized control-plane payloads or oversized on-disk session blobs fail closed instead of being read into memory unboundedly, hosted login/session/status/refresh requests now also reject non-JSON response content-types before parsing so HTML or plaintext intermediaries cannot be treated as trusted control-plane payloads, and control-plane login/session/status/refresh requests now reject HTTP redirects instead of silently following them so hosted auth and bearer-token traffic cannot be bounced to a different endpoint by transport-layer redirect behavior.

# End goal with specs

- A user can sign in on the site with GitHub or Google
- A user can pay for managed hosting with Stripe
- A paid user gets one managed Linux VM on GCP infrastructure
- A local SuperTurtle install can link to the hosted account with a browser OAuth login flow
- `/teleport` can target the managed VM without manual SSH host setup
- Teleport preserves the existing semantic handoff model and same Telegram bot identity
- The control plane tracks users, subscriptions, managed instances, cloud links, and teleport sessions
- Hosted provider credentials remain user-scoped and are never shared between users
- The hosted product is production-ready rather than demo-only: durable state, idempotent jobs, auditable actions, and clean cutover boundaries for real Stripe/GCP credentials
- Infra posture is concrete in v1: one GCP project, one primary region, one Linux VM template/image, one managed VM per paid account
- Missing live Stripe or GCP accounts are implementation constraints, not excuses for fake architecture: build production-shaped adapters, state machines, APIs, and cutover hooks now
- Implementation order matters: auth and CLI login first, then managed-instance provisioning, then entitlement/billing integration, then `/teleport` target resolution

# Roadmap (Completed)
- Existing Telegram bot runtime, Claude/Codex routing, MCP tools, queueing, and session management are shipped
- Manual teleport exists already and remains the baseline semantic handoff path to reuse
- Runtime isolation for logs, temp dirs, IPC dirs, and tmux sessions is already in place
- Current operator ergonomics exist: `/status`, `/debug`, `/looplogs`, `superturtle status`, and `superturtle logs`
- Managed teleport product framing, constraints, and execution priorities have been written into the root `CLAUDE.md`

# Roadmap (Upcoming)
- Build hosted auth, CLI browser login, device/callback completion, and control-plane identity/session foundations
- Build GCP managed-instance provisioning, bootstrap, machine registration, and cloud status paths
- Build billing and entitlement enforcement behind production-shaped interfaces without coupling core auth/provisioning to Stripe
- Extend `/teleport` to resolve managed targets from the control plane and reuse the current semantic handoff path
- Add provider setup, admin tooling, telemetry, and production hardening

# Backlog
- [ ] Design and implement the hosted auth foundation and `superturtle login` browser OAuth flow, including browser launch, callback completion, local session storage, and `whoami`/cloud status semantics <- current
  Progress: the CLI now has production-shaped `login`, `whoami`, `cloud status`, and `logout` commands backed by a durable local cloud session file, session-pinned control-plane reuse, atomic versioned session writes with `0600` permissions, fail-closed write-time schema validation before persistence, fail-closed write-time local session size validation before temp-file creation, crash-durable session writes that `fsync` the temp file before rename plus the final file and parent directory after replacement, crash-durable creation of missing session directories via parent-directory and new-directory `fsync` before any session-file rename depends on that metadata, `0700` permissions for newly created hosted session directories so fresh local cloud-session paths do not inherit permissive umask defaults, post-rename and post-unlink no-follow descriptor-based `fsync` reopening plus post-open type validation so a symlink swap cannot retarget the final durability sync, crash-durable session deletion that `fsync`s the parent directory after unlink during logout/session invalidation, logout-time parent-directory and regular-file validation so local session deletion fails closed on symlinked config directories and symlinked session paths, exclusive random temp-file creation before atomic rename so pre-created predictable temp-path symlinks cannot redirect session writes, on-read permission hardening that re-secures supported session files back to `0600`, descriptor-based no-follow permission repair so a symlink swap before read-time `0600` repair fails closed instead of retargeting `chmod` onto another file, schema-aware session reads that transparently normalize legacy pre-version files and reject unsupported future formats, explicit corruption recovery guidance, refresh-on-expiry/401 semantics, explicit local invalidation on hosted session `403` rejection as well as `401` so control-plane-forbidden bearer sessions are cleared instead of being cached and reused, persisted identity/entitlement/instance/provisioning snapshots, cached snapshot fallback for temporary control-plane outages including transient HTTP failures such as 429/5xx, socket-level disconnects, bounded request timeouts, bounded browser-launch timeouts, configurable hosted-response byte limits, configurable local session-file byte limits, fail-closed content-type validation for hosted login/session/status/refresh responses, fail-closed redirect rejection for hosted login/session/status/refresh requests so the CLI never silently follows control-plane redirects, HTTPS-only validation for configured and persisted hosted control-plane origins outside loopback so production auth/session traffic cannot be downgraded onto insecure remote HTTP endpoints, loopback-aware validation for hosted login `verification_uri` / `verification_uri_complete` values so local `http://127.0.0.1` browser/device harnesses still work while remote insecure HTTP verification endpoints are rejected, stubbed control-plane login/session/status/refresh APIs, refresh-aware cached fallback persistence so newly refreshed tokens are still written locally if the pinned control plane becomes unreachable before snapshot fetch completion, initial login snapshot persistence so browser-auth completion immediately seeds cached `whoami` and `cloud status` data before any follow-up API fetch succeeds, per-surface `identity_sync_at` / `cloud_status_sync_at` timestamps so cached fallback messaging reflects the freshness of the specific snapshot being used instead of a coarse global sync time, durable on-read migration of legacy pre-version session files, on-read backfill of missing `control_plane`, `created_at`, `last_sync_at`, `identity_sync_at`, and `cloud_status_sync_at` metadata so cached fallback remains offline-safe across older local session shapes, validation ordering that rejects unsupported future schema files before any migration write or permission repair can silently mutate them, explicit local invalidation of revoked or unauthorized hosted sessions so stale credentials are cleared from disk and the CLI forces a fresh `superturtle login`, fail-closed validation of login-start, login-completion, refresh-token, hosted-session, and cloud-status payloads so malformed control-plane responses cannot be merged into the durable local session, fail-closed validation of embedded identity/cloud-status snapshot fields in token-bearing login-completion and refresh responses before those snapshots can be persisted, fail-closed on-read validation of stored tokens, refresh timestamps, sync timestamps, and cached identity/cloud-status snapshot fields so malformed local hosted session state is rejected before any command reuses it, fail-closed validation of persisted/configured `control_plane` URLs so session reuse only targets origin-only `http`/`https` control-plane endpoints, same-origin validation of hosted login `verification_uri` / `verification_uri_complete` values so browser sign-in cannot redirect to a different host than the configured control plane, retry-aware hosted login polling so device-flow `429` / `slow_down` responses and HTTP `Retry-After` throttling both back off and continue instead of aborting login, timeout-budgeted hosted login polling so oversized `Retry-After` or `slow_down` backoff values cannot keep sleeping past the configured browser login deadline before the next poll, configurable bounded request timeouts for hosted login/session/status/refresh calls so dead control-plane connections fail fast instead of hanging the CLI indefinitely, configurable bounded browser-launch timeouts for `superturtle login --browser` so a stuck local opener cannot block device login forever, regular-file enforcement for local hosted session paths so symlinked, dangling-symlink, or directory-backed session files are rejected before read-time validation, permission repair, login writes, or logout deletion can treat them as safe local session files, parent-directory validation so hosted session reads/writes/logout also reject symlinked config directories instead of traversing them, no-follow descriptor-based session reads plus post-open `fstat` validation so a symlink swap between validation and file open fails closed instead of being followed on supported platforms, and bounded descriptor-level session-file reads so a concurrent file-growth race after the initial `fstat` size check still trips the configured local session-file byte cap instead of reading an oversized blob into memory.
- [ ] Define and implement the control-plane schema, APIs, and durable state transitions for users, identities, sessions, entitlements, managed instances, provisioning jobs, and audit log
- [ ] Build GCP managed VM provisioning, bootstrap, registration, health reporting, and idempotent reprovision behavior for one VM per paid account
- [ ] Add Stripe checkout, subscriptions, webhook processing, and entitlement enforcement behind production-shaped adapters and verified webhook handling
- [ ] Extend `/teleport` to resolve and use SuperTurtle-managed VM targets from the control plane
- [ ] Add hosted Claude auth setup and validation flow with user-scoped credential boundaries
- [ ] Add basic admin and support tooling for reprovision, suspend, instance inspection, and teleport audit
- [ ] Add production telemetry for provisioning failures, login failures, teleport failures, and unhealthy VMs
