## Current Task
Update post render component for image grid layout.

## End Goal with Specs
- Posts can include 1–4 images.
- Feed renders a grid layout based on image count.

## Backlog
- [x] Extend posts schema to support `imageStorageIds` array.
- [x] Update create post flow to upload multiple images (max 4).
- [ ] Update post render component for image grid layout. <- current
- [ ] Commit: `add multi-image posts`

## Notes
- Build on single-image support if present.
